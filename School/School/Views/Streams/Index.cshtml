@model IEnumerable<School.Models.Stream>

@{
    ViewBag.Title = "Index";
}

<h2>Index</h2>

<p>
    @Html.ActionLink("Create New", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.StreamName)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.StreamSubjects)
        </th>
        <th>
            @Html.DisplayNameFor(model => model.PreCompulsorySubjects)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.StreamName)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.StreamSubjects)
        </td>
        <td>
            @Html.DisplayFor(modelItem => item.PreCompulsorySubjects)
        </td>
        <td>
            @Html.ActionLink("Edit", "Edit", new { id=item.Id }) |
            @Html.ActionLink("Details", "Details", new { id=item.Id }) |
            @Html.ActionLink("Delete", "Delete", new { id=item.Id })
        </td>
    </tr>
}

</table>
